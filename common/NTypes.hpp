#include <QtGlobal>

namespace NTypes
{
    using tIdType = quint32;
    using tMatIdType = unsigned int;

    using tMagicNumberType = quint32;
    using tVersionType = quint32;

    using tSizeType = qint32;
    using tAnsver = quint32;
    using tHash = quint64;
    using tHashPosition = qint32;

    using tBuffSizeTmp = qint32;
    using tMemUssage = qint32;

    using tCeserialData = float;
};
